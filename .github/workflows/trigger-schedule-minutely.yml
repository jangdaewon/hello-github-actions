name: Trigger - schedule minutely

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Show UTC time
        run: date -u
      - name: Print message
        run: echo "Hello from scheduled workflow"